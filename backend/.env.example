#
# ============================================================================
# GULLYESPORTS - Environment Variables
# ============================================================================
# Purpose: Store sensitive configuration values that should NEVER be
#          committed to version control (git).
#
# Setup Instructions:
#   1. Copy this file to ".env" in the same directory
#   2. Replace the placeholder values with your actual credentials
#   3. Never share or commit the .env file
#
# Gmail App Password Setup:
#   1. Go to: https://myaccount.google.com/apppasswords
#   2. Select App: "Mail" and Device: "Other" (name it "GULLYESPORTS")
#   3. Copy the 16-character password to EMAIL_PASS below
#   4. You must have 2-Step Verification enabled for App Passwords to work
# ============================================================================

# --- Server Configuration ---
PORT=5000

# --- MongoDB Connection ---
# Local MongoDB:
# MONGO_URI=mongodb://localhost:27017/gullyesports
#
# MongoDB Atlas (cloud):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/gullyesports
MONGO_URI=mongodb://localhost:27017/gullyesports

# --- Email Configuration (Gmail SMTP) ---
# The Gmail account that will SEND the email notifications
EMAIL_USER=ishukriitpatna@gmail.com

# Gmail App Password (NOT your regular Gmail password)
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_PASS=your_gmail_app_password_here

# The email address that will RECEIVE contact form submissions
EMAIL_TO=ishukriitpatna@gmail.com

# --- CORS Configuration ---
# Frontend URL allowed to access the API
CORS_ORIGIN=http://localhost:5173
